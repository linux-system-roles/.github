present_files:
  - .github/dependabot.yml
  - .github/pull_request_template.md
  - .fmf/version
  - .markdownlint.yaml
  - .pandoc_template.html5
  - .ostree/get_ostree_data.sh
  - .ostree/README.md
  - README-ostree.md
  - library/sr_fingerprint.py
  - tests/unit/test_sr_fingerprint.py
present_templates:
  - .ansible-lint
  - .coderabbit.yaml
  - .codespellrc
  - .yamllint.yml
  - contributing.md
  - .github/workflows/ansible-lint.yml
  - .github/workflows/ansible-managed-var-comment.yml
  - .github/workflows/ansible-test.yml
  - .github/workflows/build_docs.yml
  - .github/workflows/changelog_to_tag.yml
  - .github/workflows/codespell.yml
  - .github/workflows/markdownlint.yml
  - .github/workflows/pr-title-lint.yml
  - .github/workflows/qemu-kvm-integration-tests.yml
  - .github/workflows/test_converting_readme.yml
  - .github/workflows/tft.yml
  - .github/workflows/tft_citest_bad.yml
  - .github/workflows/weekly_ci.yml
  - .github/workflows/woke.yml
  - README-ansible.md
  - plans/README-plans.md
  - plans/test_playbooks_parallel.fmf
  - tests/vars/rh_distros_vars.yml
  - tests/tasks/run_role_with_clear_facts.yml
absent_files:
  - .commitlintrc.js
  - .github/workflows/ansible-plugin-scan.yml
  - .github/workflows/commitlint.yml
  - .github/workflows/tox.yml
  - README-devel.md
  - README-testing.md
  - .github/actions/custom-woke-action/LICENSE
  - .github/actions/custom-woke-action/README.md
  - .github/actions/custom-woke-action/action.yml
  - .github/actions/custom-woke-action/entrypoint.sh
  - .github/actions/custom-woke-action/testdata/bad.txt
  - .github/actions/custom-woke-action
  - .github/actions
  - .commitlintrc.js
github_org: linux-system-roles
github_url_prefix: "git@github.com:"
default_ansible_lint:
  skip_list:
    - fqcn-builtins
    - var-naming[no-role-prefix]
  warn_list: []
  exclude_paths:
    - tests/roles/
    - .github/
    - .markdownlint.yaml
    - examples/roles/
    - .collection/
  mock_roles:
    - linux-system-roles.{{ inventory_hostname }}
lsr_namespace: fedora
lsr_name: linux_system_roles
lsr_role_namespace: linux_system_roles  # for ansible-lint
gha_checkout_action: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
tox_lsr_url: "git+https://github.com/linux-system-roles/tox-lsr@d594be24ed56e586a5796720d51c261e40c20496"  # 3.20.1
# These are the supported RHEL-alike distros - note that at the time of writing,
# OracleLinux has some problems with some roles, so we don't include it here
# Roles that do support OracleLinux can list it in lsr_rh_distros_extra
lsr_rh_distros: "{{ ['AlmaLinux', 'CentOS', 'RedHat', 'Rocky'] + lsr_rh_distros_extra | d([]) }}"
# The ansible.posix and community.general versions are intentionally left
# unconstrained so that all roles track the latest releases. Compatibility with
# older managed nodes is preserved by vendoring modules into the roles that need
# them and selecting, per task, either the vendored copy or the latest
# collection module based on the managed node's python version: the latest
# community.general requires python >= 3.7 and the latest ansible.posix requires
# python 3, so nodes below those thresholds use the vendored copies. This also
# covers ansible 2.9 managed nodes (e.g. EL7, python 2), where the collections
# cannot be installed at all (tft-tests likewise skip collection installation on
# ansible 2.9). The previous caps (ansible.posix <2.2.0, community.general
# <12.0.0) are therefore no longer needed. If a role ever needs to pin a
# version, it can add a version to the entry in the role's host_vars file.
